import django.contrib.admin.sites

class RegisterAdmin(django.contrib.admin.sites.AdminSite):
    site_header = 'Register Administration'
    site_title = 'Register Admin'
    index_title = 'Register Administration'
